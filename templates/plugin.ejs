<%if (hasUtil) {%>const { /*get somthing*/ } = kb2abot.utils["<%=name%>"];

<%}%>module.exports = {
	authorDetails: {	// Thông tin tác giả
		name: "xxx",				// Tên tác giả của plugin này
		contact: "fb.com/xxx"			// Liên hệ (có thể là link fb, gmail, bla bla)
	},

	friendlyName: "Plugin that do awesome things",
	// Tên thân thiện của plugin (hiển thị trong danh sách câu lệnh)

	keywords: ["<%=name%>"],
	// Là các từ khóa để gọi plugin <%=name%> (có thể có nhiều keyword)

	description: "This plugin is awesome!",
	// Là nội dung của plugin (hiển thị trong <prefix>help <tên lệnh>)

	extendedDescription: " ",
	// Là hướng dẫn sử dụng của plugin (hiển thị trong <prefix>help <tên lệnh>)

	hideFromHelp: false,
	// Ẩn plugin khỏi help
	// true là ẩn, còn false là hiện

	onLoad: async function() {
		// Được gọi khi plugin load xong
	},

	onMessage: async function(api, message) {
		// Được gọi mỗi khi có message nhắn tới (kể cả khi dùng lệnh)
		// Lưu ý: onMessage không được gọi khi member xài lệnh của mình (xem onCall)
	},

	onCall: async function(api, message) {
		// Được gọi khi có member xài lệnh của mình
		// Note: onCall và onMessage không bao giờ chạy cùng 1 lúc
	}
};
